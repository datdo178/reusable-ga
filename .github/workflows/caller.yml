name: Caller

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
  workflow_dispatch:

runs-on: ubuntu-latest

jobs:
  call:
    uses: datdo178/github-actions/.github/workflows/called.yml@main
